const chai = require("chai");
const { assert } = chai;
const VersionManeger = require("./");

describe("Find Version", () => {
  it("Local Find", async () => {
    const LocalVersion = VersionManeger
  });
});